language: node_js
node_js:
- node
cache:
  directories:
  - node_modules
script:
- npm test
deploy:
  provider: script
  script: bash scripts/deploy.sh
  on:
    branch: master
before_install:
- openssl aes-256-cbc -K $encrypted_fceaa16ce1a1_key -iv $encrypted_fceaa16ce1a1_iv
  -in deploy.key.enc -out deploy.key -d
